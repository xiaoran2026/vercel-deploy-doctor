# Deploy Doctor 推广文案

> 产品定位：给 Vercel 部署做"体检"的免费工具，30 秒生成 40+ 项检测报告，无需注册无需安装。
> 域名：https://www.store-leak.com
> 早鸟福利：Waitlist 用户所有付费套餐永久 37% off

---

## 1. Twitter / X 长推（主推）

```
Your Vercel deploy looks fine.

Until a customer in Germany can't load it. 🧊

I built Deploy Doctor — paste any Vercel URL, get a 40+ point deploy audit in under 30 seconds:

✅ Security headers (CSP / HSTS / X-Frame)
✅ Cache-Control + Vercel edge cache hit ratio
✅ SEO meta / og:image / canonical
✅ Accessibility baseline
✅ vercel.json hygiene + best practices

No signup. No Vercel token. No CLI install.
Each finding comes with the exact code snippet to fix it.

Free during pre-launch. Early-bird members get 37% off forever.

Try it → store-leak.com

#Vercel #NextJS #WebPerformance #IndieHackers
```

---

## 2. Indie Hackers 帖子

**标题**：`I open-sourced my Vercel deploy — 11 critical findings. Now I'm fixing them in public.`

**正文**：

```
Last week I shipped a Next.js app to Vercel. It looked great.

Then I ran a deploy audit and found:
- Missing Content-Security-Policy header (XSS attack surface)
- No Cache-Control on the root page (every visitor hits origin)
- og:image missing (social shares render blank)
- No <html lang> attribute (screen readers guess wrong)
- X-Powered-By leaking the stack

All on a "production-ready" deploy.

So I built Deploy Doctor.

Paste any Vercel URL → get a 40+ point audit in 30 seconds. Security headers, caching, vercel.json hygiene, SEO, a11y — each finding comes with the exact code snippet to fix it.

- No signup required (guest scan works)
- No Vercel token or CLI install
- Free during pre-launch
- Early-bird waitlist gets 37% off all paid plans forever

If you ship to Vercel, I'd love your feedback: store-leak.com
```

---

## 3. V2EX 帖子

**标题**：`[工具] 给 Vercel 部署做体检 - 30 秒生成 40+ 项检测报告，无需注册`

**正文**：

```
最近给 Vercel 部署做了个"体检工具"，解决的问题很简单：

你部署完看起来一切正常，但用户在德国打不开、
Google 收录不到、X 分享卡片是空白的——
这些"静默故障"很难自己发现。

**Deploy Doctor 怎么用：**
1. 打开 store-leak.com
2. 粘贴你的 Vercel URL
3. 30 秒拿到 40+ 项检测报告

**检测范围：**
- 安全 headers（CSP / HSTS / X-Frame-Options）
- Cache-Control + Vercel 边缘缓存命中
- SEO meta / og:image / canonical
- 无障碍基线（lang / alt / h1）
- vercel.json 配置审计

**特点：**
- 不需要注册（游客模式直接扫）
- 不需要 Vercel token 或安装 CLI
- 每条 finding 都附带修复代码片段
- 上线期间免费，早鸟用户所有付费套餐永久 37% off

欢迎试用提建议：store-leak.com

技术栈：Next.js 15 + Prisma + Neon Postgres + Vercel
```

---

## 4. Product Hunt 发布（待正式 Launch）

**Tagline**：`The deploy audit tool Vercel forgot to build`

**Description**：

```
Deploy Doctor scans any public Vercel URL and returns a 40+ point deploy audit in under 30 seconds — before your customers find the silent regressions.

🔍 What it checks:
- Security headers (CSP, HSTS, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy)
- Cache-Control and Vercel edge cache behavior
- SEO fundamentals (title, meta description, og:image, canonical, robots)
- Accessibility baseline (html lang, img alt, h1 count)
- Performance heuristics (blocking scripts, HTML size, inline styles)
- vercel.json best practices and configuration hygiene

🛠 How it works:
- Paste any public Vercel URL (your-app.vercel.app or custom domain)
- No Vercel token, no CLI install, no GitHub app
- Each finding includes the exact code snippet to fix it
- Guest scan works without signup

💼 Pricing:
- Free: 3 scans/month, all checks included
- Starter ($19/mo): 50 scans, AI fix suggestions, trend history
- Growth ($49/mo): Unlimited, scheduled checks, Slack alerts, API access
- Agency ($149/mo): White-label, multi-project, client dashboard

🎉 Early-bird: Waitlist members get 37% off all paid plans forever.
```

---

## 5. 简短评论区版本（Reddit / HN / 群里发）

**英文版**：

```
I built a free tool that audits Vercel deploys — paste your URL, get a 40+ point report (security headers, caching, SEO, a11y, vercel.json) with fix snippets in 30s. No signup needed: store-leak.com
```

**中文版**：

```
做了个给 Vercel 部署体检的免费工具，粘贴 URL 30 秒出 40+ 项检测报告（安全 headers / 缓存 / SEO / 无障碍 / vercel.json），每条都带修复代码片段，不用注册：store-leak.com
```

---

## 发布节奏建议

| 渠道 | 时机 | 备注 |
|------|------|------|
| V2EX | 今天 | 中文开发者聚集，反馈快 |
| Twitter/X | 今天 | 配截图发长推 |
| Indie Hackers | 今天或明天 | 社区氛围匹配 |
| 朋友群/开发者群 | 今天 | 熟人冷启动 |
| Reddit r/nextjs r/webdev | 观察 1-2 天后 | 看反馈再投 |
| Product Hunt | 收集 20+ Waitlist 后 | 一次性爆发 |

## 观察指标

发布后 24-48 小时重点看：
- **UV / PV**（Vercel Analytics）
- **扫描次数**（Admin Stats 页 /api/admin/stats?key=6b0a5369）
- **Waitlist 邮箱数**
- **注册用户数**

最低及格线：UV 100+ / 扫描 10+ / Waitlist 5+，没达到就优化文案或渠道。
